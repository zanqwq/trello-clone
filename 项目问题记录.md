项目问题记录 : 

drag and drop api在dataTransfer中setData时, 被stringify的state在parse后变为unobservable, 解决方法为在data中声明dataTransfer. 但是重构后又有问题, 所以还是采用避免stringify state的方法来使用e.dataTransfer.setData

templates和boards页拥有大概相同的结构, 抽取公共view页面

使用本地存储时, 需要添加`currentUserEmail`本地存储, 如果用户用这个当email名应该会出问题.

login enter 无效？？？

```vue
<template>
  <div>
    <div class="wrapper mx-auto my-5 text-center">
      <h1>Trello clone</h1>

      <b-form class="form my-5 p-3 border-secondary shadow">
        <h6 class="mb-3">Log in to Trello clone</h6>
        <b-form-input
          class="my-3"
          type="email"
          required
          placeholder="Enter email"
          v-model="email"
          @focus="accountIsExist = true"
        ></b-form-input>
        <p class="text-danger" v-if="!accountIsExist">
          this account is not exist
        </p>
        <b-button
          class="my-3"
          block
          :disabled="!email"
          variant="success"
          @click="login"
        >
          Login
        </b-button>
        <p class="my-3 text-muted">OR</p>
        <b-button class="my-3" block>Login with Google</b-button>
        <b-button class="my-3" block>Login with Microsoft</b-button>
        <hr />
        <b-link :to="{ name: 'signup' }">Sign up for an account</b-link>
      </b-form>

      <p></p>
    </div>
    <Footer class="footer position-absolute" />
  </div>
</template>

<script>
import Footer from "@/components/Footer.vue";
export default {
  data() {
    return {
      email: "",
      accountIsExist: true
    };
  },
  methods: {
    login() {
      if (!this.email) {
        return;
      }

      // let user = JSON.parse(localStorage.getItem(this.email));
      if (!localStorage.getItem(this.email)) {
        this.accountIsExist = false;
        return;
      }

      this.accountIsExist = true;
      this.$store.commit("LOGIN_USER", { email: this.email });
      // this.$router.push({
      //   name: "user-boards",
      //   params: { username: this.username }
      // });
    }
  },
  components: {
    Footer
  }
};
</script>

<style scoped>
.wrapper {
  max-width: 400px;
}

.footer {
  bottom: 0px;

  margin-left: 50%;
  transform: translateX(-50%);
}
</style>

```



